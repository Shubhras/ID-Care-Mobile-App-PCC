import {StyleSheet} from 'react-native';
import { FONT_SIZE_XS, OPEN_SANS_BOLD } from '../../../constants/Constants';
 
// Exporting style
export default StyleSheet.create({
  style: {
    fontFamily: OPEN_SANS_BOLD,
    fontSize: FONT_SIZE_XS,
    textDecorationStyle:'solid'
  },
});
